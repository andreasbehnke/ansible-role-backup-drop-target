---
  backup_target_user: "backup"
  backup_target_group: "backup"
  backup_target_user_home: "/home/{{backup_target_user}}"
  backup_target_keys_path: "{{backup_target_user_home}}/keys"
  backup_target_ssh_path: "{{backup_target_user_home}}/.ssh"
  backup_target_authorized_keys: "{{backup_target_ssh_path}}/authorized_keys"
  backup_target_target_path: "/srv/backup"